name: 'sample_project'
version: '1.0.0'
config-version: 2

profile: 'sample_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  sample_project:
    staging:
      +materialized: view
    marts:
      +materialized: table
